const fs = require("fs");
const path = require("path");

getRenamers = function () {
	/*var renamersFilePath = path.join(__dirname, '../', 'renamers.json')
	var data = fs.readFileSync(renamersFilePath, 'utf8');
	var renamersJson = JSON.parse(data);

	renamersJson.forEach(function(renamer, index){
		console.log(renamer.name);
		console.log(renamer.renameColumns);
	})


	const url = 'https://api.myjson.com/bins/7xq2x';

	// Populate dropdown with list of provinces
	$.getJSON(url, function (data) {
		$.each(data, function (key, entry) {
			dropdown.append($('<option></option>').attr('value', entry.abbreviation).text(entry.name));
		})
	});

	console.log(data);*/
}
